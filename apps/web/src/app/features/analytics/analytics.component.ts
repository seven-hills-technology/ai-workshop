import { Component } from '@angular/core';

/**
 * Workshop section 5. Use this module as a sandbox for the `ship` skill
 * exercise — make a change here, then invoke the skill.
 */
@Component({
  selector: 'app-analytics',
  standalone: true,
  template: `
    <h2>Analytics</h2>
    <p>Section 5 sandbox — wire a skill into your commit flow against changes here.</p>
  `,
})
export class AnalyticsComponent {}
