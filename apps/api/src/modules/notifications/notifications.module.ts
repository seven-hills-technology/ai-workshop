import { Module } from '@nestjs/common';

/**
 * Workshop section 3 exercise: build this module from docs/specs/notifications.md.
 * Controller, service, and types are intentionally missing — that's the exercise.
 */
@Module({})
export class NotificationsModule {}
